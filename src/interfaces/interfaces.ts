export interface NavigationInterface {
    name: string,
    path: string
}

export interface ButtonInterface {
    text: string,
    pathname: string,
    bgColor?: string,
    playIcon?: boolean,
    arrowIcon?: boolean
}